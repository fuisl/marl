defaults:
  - override /algo: sac
  - override /scenario: cologne8
  - override /model: gat
  - override /train: default
  - _self_

tags: [cologne8, gat]
runtime:
  out_dir: runs/cologne8_seed${seed}
