defaults:
  - env: sumo
  - scenario: grid5x5
  - model: default
  - _self_

runtime:
  checkpoint_path: null
  episodes: 5
  device: cuda
  gui: false

hydra:
  run:
    dir: .
  output_subdir: null
